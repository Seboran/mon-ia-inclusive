name: CI on push

on:
  push:
permissions:
  contents: read # for checkout
jobs:
  test:
    uses: ./.github/workflows/test-on-call.yml

  release:
    needs: [test]
    uses: ./.github/workflows/release.yml
    secrets:
      PAT: ${{ secrets.PAT }}
